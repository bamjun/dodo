handle = open("mbox-short.txt")
dodo = dict()
for x in handle:
    x.rstrip()
    if not x.startswith("From ") : continue
    bobo = x.split()
    nono = bobo[5].split(':')
    dodo[nono[0]] = dodo.get(nono[0], 0) + 1
dodo = sorted(dodo.items())
for k, v in dodo:
    print(k, v)
