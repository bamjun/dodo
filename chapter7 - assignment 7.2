# Use the file name mbox-short.txt as the file name
fname = input("Enter file name: ")
fh = open(fname)
x = 0
y = 0
for line in fh:
    if not line.startswith("X-DSPAM-Confidence:") : continue
    x = x + float(line[line.find('0.'):])
    y = y + 1
print("Average spam confidence:", x/y)
